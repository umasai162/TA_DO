services:
  - type: web
    name: todo-app
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt && npm install --prefix frontend && VITE_API_URL=/ npm run build --prefix frontend
    startCommand: cd backend && gunicorn main:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: ALLOWED_ORIGINS
        value: https://todo-app-yfby.onrender.com,http://localhost:3000,http://localhost:5173
